class Solution:
    def losingPlayer(self, x: int, y: int) -> str:
        count=0
        while x>=1 and y>=4:
            count+=1
            x=x-1
            y=y-4
        return "Alice" if count%2==1 else "Bob"

        
